import React from 'react'; import Header from '../components/Header'; import { useEffect, useState } from 'react'; import styles from '../styles/Home.module.css'; export default function Home() { const [activities, setActivities] = useState([]); useEffect(() => { fetch('/api/activities') .then(res => res.json()) .then(data => setActivities(data)) .catch(err => console.error(err)); }, []); return ( <div className={styles.container}> <Header /> <h1>Welcome to Holifit</h1> <h2>Your Fitness Journey</h2> <ul> {activities.map(activity => ( <li key={activity.id}>{activity.name}: {activity.progress}</li> ))} </ul> </div> ); }